# Runtime dependencies (boto3 is included in AWS Lambda runtime, listed here for local dev)
boto3>=1.35.0

# Testing & Quality
pytest>=8.0.0
pytest-mock>=3.14.0
moto[dynamodb,sqs,s3]>=5.0.0
ruff>=0.8.0
